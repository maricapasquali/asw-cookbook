export function add_like(req, res){ return res.status(501).send('add like on recipe')}
export function remove_like(req, res){ return res.status(501).send('remove like on recipe')}
export function list_likes(req, res){ return res.status(501).send('list liker of recipe')}